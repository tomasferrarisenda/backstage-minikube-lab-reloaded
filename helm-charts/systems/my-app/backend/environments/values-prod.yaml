environment: prod

deployment:
  image:
    tag: backstage-minikube-lab
  replicas: 1

  labels:
    environment: prod
    app: my-app-backend-prod
    backstage.io/kubernetes-id: my-app-backend-prod

  env:
    redis_host: redis-prod-master 
